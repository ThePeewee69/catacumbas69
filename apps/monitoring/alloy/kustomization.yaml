apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: alloy-config
    namespace: monitoring
    files:
      - config.alloy
    options:
      disableNameSuffixHash: true
resources:
  - loki-agent-rbac.yaml
  - alloy-daemonset.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-media-data
  namespace: media
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  volumeName: "sonarr-media-data-pv"
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Ti

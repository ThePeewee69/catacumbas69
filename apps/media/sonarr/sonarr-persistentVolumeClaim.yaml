apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-monitoring-config
  namespace: monitoring
spec:
  volumeName: "sonarr-monitoring-pv"
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi

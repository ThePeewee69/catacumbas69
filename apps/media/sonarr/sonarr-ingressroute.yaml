apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr
  namespace: media
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/sonarr`)
      kind: Rule
      middlewares:
        - name: sonarr-headers
      services:
        - name: sonarr
          port: 8989

